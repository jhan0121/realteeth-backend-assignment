spring:
  application:
    name: image-processing
  datasource:
    url: jdbc:mysql://mysql:3306/image_processing?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: realteeth
    password: realteeth
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
